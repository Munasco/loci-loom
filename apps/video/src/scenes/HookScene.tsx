import { AbsoluteFill, Easing, interpolate, useCurrentFrame, useVideoConfig } from 'remotion';
import { Eyebrow, palette } from '../design';
export const HookScene = () => { const frame = useCurrentFrame(); const { fps } = useVideoConfig(); return <AbsoluteFill style={{ background: palette.paper, justifyContent: 'center', padding: '100px 110px' }}><div style={{ opacity: interpolate(frame, [0, .5 * fps], [0, 1], { extrapolateRight: 'clamp' }) }}><Eyebrow>Introducing Loci Loom</Eyebrow></div><div style={{ color: palette.ink, fontSize: 112, lineHeight: .98, fontWeight: 950, letterSpacing: -6, maxWidth: 1300, opacity: interpolate(frame, [8, 28], [0, 1], { extrapolateRight: 'clamp' }), translate: `0 ${interpolate(frame, [8, 28], [60, 0], { extrapolateRight: 'clamp', easing: Easing.bezier(.16, 1, .3, 1) })}px` }}>Turn anything you study into a world you can walk through.</div><div style={{ position: 'absolute', right: 110, bottom: 90, color: palette.coral, fontSize: 92, rotate: `${interpolate(frame, [0, 90], [-8, 8], { extrapolateRight: 'clamp' })}deg` }}>✦</div><div style={{ position: 'absolute', left: 110, bottom: 92, color: palette.muted, fontSize: 32 }}>Memory that feels like exploration—not repetition.</div></AbsoluteFill>; };
