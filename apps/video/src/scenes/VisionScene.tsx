import { AbsoluteFill, interpolate, useCurrentFrame } from 'remotion';
import { palette } from '../design';
export const VisionScene = () => { const frame = useCurrentFrame(); return <AbsoluteFill style={{ background: palette.ink, alignItems: 'center', justifyContent: 'center', color: palette.white }}><div style={{ color: palette.coral, fontSize: 34, fontWeight: 950, letterSpacing: 4, opacity: interpolate(frame, [0, 18], [0, 1], { extrapolateRight: 'clamp' }) }}>LOCI LOOM</div><div style={{ fontSize: 93, lineHeight: 1.04, fontWeight: 950, textAlign: 'center', maxWidth: 1300, marginTop: 25, letterSpacing: -4 }}>A world for every idea.<br /><span style={{ color: palette.lavender }}>A path back to every memory.</span></div><div style={{ marginTop: 55, fontSize: 29, color: '#B9B8C5' }}>Built and tested for the RevenueCat Next Gen student track.</div><div style={{ position: 'absolute', bottom: 70, display: 'flex', gap: 18 }}>{['Expo', 'RevenueCat', 'Remotion'].map(item => <div key={item} style={{ border: '2px solid #424455', padding: '11px 20px', borderRadius: 999, fontSize: 20, fontWeight: 800 }}>{item}</div>)}</div></AbsoluteFill>; };
