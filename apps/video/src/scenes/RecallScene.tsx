import { AbsoluteFill, interpolate, useCurrentFrame } from 'remotion';
import { Eyebrow, palette, Phone } from '../design';
export const RecallScene = () => { const frame = useCurrentFrame(); const points = ['Active recall after every trail', 'Stronger prompts when memory fades', 'A growing map of what you know']; return <AbsoluteFill style={{ background: palette.plum, color: palette.white, padding: '92px 110px', overflow: 'hidden' }}><div style={{ position: 'absolute', left: 100, top: 100 }}><Phone image="qa-mobile-recall.png" rotate={-3} scale={1.02} /></div><div style={{ marginLeft: 590 }}><Eyebrow>Learning that adapts</Eyebrow><div style={{ fontSize: 79, lineHeight: 1.02, fontWeight: 950, letterSpacing: -3 }}>Don’t reread it.<br />Retrieve it.</div><div style={{ marginTop: 46, display: 'flex', flexDirection: 'column', gap: 25 }}>{points.map((point, index) => <div key={point} style={{ display: 'flex', alignItems: 'center', gap: 20, fontSize: 30, opacity: interpolate(frame, [20 + index * 14, 34 + index * 14], [0, 1], { extrapolateRight: 'clamp' }), translate: `${interpolate(frame, [20 + index * 14, 34 + index * 14], [40, 0], { extrapolateRight: 'clamp' })}px 0` }}><span style={{ width: 38, height: 38, borderRadius: 19, background: palette.mint, color: '#29553A', display: 'inline-flex', alignItems: 'center', justifyContent: 'center', fontWeight: 900 }}>✓</span>{point}</div>)}</div></div></AbsoluteFill>; };
