import { Img, interpolate, staticFile, useCurrentFrame } from 'remotion';
export const palette = { ink: '#171828', paper: '#F7F4EC', plum: '#542B66', coral: '#FF7657', lavender: '#DCC8F2', white: '#FFFFFF', muted: '#727180', mint: '#C8E6D0' };
export const Phone = ({ image, x = 0, rotate = 0, scale = 1 }: { image: string; x?: number; rotate?: number; scale?: number }) => { const frame = useCurrentFrame(); return <div style={{ width: 310, height: 672, borderRadius: 48, padding: 11, background: '#12131D', boxShadow: '0 36px 90px rgba(29,18,40,.32)', translate: `${x}px ${interpolate(frame, [0, 24], [40, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}px`, rotate: `${rotate}deg`, scale }}><div style={{ width: '100%', height: '100%', borderRadius: 38, overflow: 'hidden', position: 'relative', background: palette.paper }}><Img src={staticFile(`screens/${image}`)} style={{ width: '100%', height: '100%', objectFit: 'cover' }} /></div></div>; };
export const Eyebrow = ({ children }: { children: React.ReactNode }) => <div style={{ color: palette.coral, fontSize: 25, fontWeight: 900, letterSpacing: 5, textTransform: 'uppercase', marginBottom: 20 }}>{children}</div>;
export const Pill = ({ children }: { children: React.ReactNode }) => <div style={{ display: 'inline-flex', alignItems: 'center', padding: '12px 18px', borderRadius: 999, background: palette.lavender, color: palette.plum, fontSize: 22, fontWeight: 800 }}>{children}</div>;
